import { Options } from "./options";
/**
 * millify converts long numbers to human-readable strings.
 */
declare function millify(value: number, options?: Partial<Options>): string;
export { millify };
export default millify;
